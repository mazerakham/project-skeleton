const { React, ReactDOM } = window;
const {
  cloneElement,
  createContext,
  createElement,
  forwardRef,
  Fragment,
  useCallback,
  useContext,
  useEffect,
  useLayoutEffect,
  useMemo,
  useReducer,
  useRef,
  useState,
} = React;

const { createPortal } = ReactDOM;

export {
  cloneElement,
  createContext,
  createElement,
  createPortal,
  forwardRef,
  Fragment,
  React,
  ReactDOM,
  useCallback,
  useContext,
  useEffect,
  useLayoutEffect,
  useMemo,
  useReducer,
  useRef,
  useState,
};
